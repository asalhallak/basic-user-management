/**
 * Barrel re-exports for shared UI components declared in `AppModule`.
 *
 * `AlertComponent` is rendered once in `app.component.html` and subscribes to `AlertService`.
 *
 * @see docs/front-end-alerts.md
 * @see docs/front-end-shell.md
 */
export * from './alert.component';
